Write-Host "This package is designed for GitHub Actions and does not require a permanently running Windows PC."
Write-Host "Create a new GitHub repository, upload the package contents, then enable Actions."
Write-Host "For a local test: py -m pip install -r requirements.txt; py -m pytest -q; py src/collector.py"
